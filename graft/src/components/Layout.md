# src/components/Layout.jsx

- Layout · function · L27-L250 — function Layout({ subtopic, onProfileClick, children, round, nodescription = false, onBackClick, showBackButton = true, allowScroll = false, hostmessage = false, popupStep = null, sidebarExtra = null, showSidebar = true, // ✅ 사이드바 표시 여부 제어 프롭 추가 (기본값 true) })
- syncMateName · function · L62-L87 — syncMateName = async ()
- getTranslatedValue · function · L93-L98 — getTranslatedValue = (raw)
- getSidebarRoleName · function · L100-L122 — getSidebarRoleName = (player)
- onResize · function · L137-L142 — onResize = ()
- getVoiceStateForRoleWithMyStatus · function · L153-L157 — getVoiceStateForRoleWithMyStatus = (roleId)
